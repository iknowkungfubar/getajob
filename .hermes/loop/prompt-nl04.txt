Read /home/turin/getajob/approval_queue/routes.py fully (it's 800+ lines).

Find ALL places where mock data is returned instead of real DB data. The known mock data generators are:
- _mock_stats() — fallback when DB is None
- _mock_applications() — fallback when DB is None
- _mock_application_detail() — fallback when DB is None

Fix the flow so that:

1. When the DB has data, show REAL data (which already works for dashboard and list)
2. When the DB is not available (db is None or tables don't exist), return the mock data with a clear "demo mode" banner

But the KEY issue is: the _fetch_application_detail function (used by the review page) might return mock data even when real data exists. Check if this is the case and fix it.

Specifically:
- Check if _fetch_application_detail queries the DB FIRST, and only falls back to _mock_application_detail when the application isn't found
- If it queries mock BEFORE DB, reverse the order

Also, add a "demo mode" warning that's displayed in the dashboard and review templates when mock data is being shown. The simplest way: add a `demo_mode` boolean to the template context.

After reviewing and fixing, run: python3 -m pytest tests/ -q --tb=short
