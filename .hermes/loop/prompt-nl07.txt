Run a targeted ruff cleanup on /home/turin/getajob. Current errors:

15 E402: module-import-not-at-top-of-file
12 ARG002: unused-method-argument
 3 RUF001: ambiguous-unicode-character-string
 1 UP046: non-pep695-generic-class

Fix only the following categories:

1. RUF001 (ambiguous unicode): Replace the 3 ambiguous unicode characters with ASCII equivalents.

2. UP046 (non-pep695): Fix the one remaining class using `class Foo[T]` syntax to use `Generic[_T]` instead.

3. E402 (module-import): Move imports to the top of the file where it's safe to do so. Skip any E402 that exists for a good reason (lazy import for graceful degradation).

4. ARG002 (unused-method-argument): These are already prefixed with underscore intentionally — skip these.

Target: <= 20 errors.

After fixing, run: ruff check . --statistics
Then run: python3 -m pytest tests/ -q --tb=short
