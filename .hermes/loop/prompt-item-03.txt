Read /home/turin/getajob/getajob/cli.py to understand the Typer app. Then add a new `doctor` CLI command that runs a comprehensive pre-flight check and reports the status of each dependency.

The `doctor` command should run these checks and print a table of results:

1. **Python version** — is it >= 3.12? Report version.
2. **LLM API key** — is GETAJOB_LLM__API_KEY or ANTHROPIC_API_KEY set in env or .env? Report which provider.
3. **Database** — can we connect? Try connecting with the configured engine. Report host/port/database.
4. **Config files** — does .env exist? Does config/settings.yaml exist? Does env.template exist?
5. **Browser** — is Playwright/Chromium installed? Try `import playwright.sync_api`. Report found/not found.
6. **Data directories** — do the data directories exist (data/, data/profiles, data/resumes, data/cover_letters)?
7. **Docker check** — is docker available? (optional, only if `which docker` works)

Format as a rich Table with columns: Check | Status (✅/❌/⚠) | Detail

If all checks pass, print a success summary. If any critical checks (API key, database) fail, print "Run `getajob init` to set up your configuration" at the end.

Use the existing `console` and `err_console` from cli.py. Use rich.table.Table and rich.panel.Panel.

Add the `doctor` command to the existing `app` Typer instance.

After adding, run: python3 -m pytest tests/ -q --tb=short
