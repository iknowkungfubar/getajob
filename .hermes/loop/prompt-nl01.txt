Read /home/turin/getajob/getajob/cli.py and /home/turin/getajob/config/settings.yaml to understand the current search vector structure.

Then add two new CLI commands to the Typer app:

1. `getajob vector list` — Lists all configured search vectors from config/settings.yaml
   - Show name, keywords, location, remote preference for each vector
   - Use rich.table.Table for formatted output
   - If no search vectors exist, print "No search vectors configured. Run 'getajob init' to set up defaults."

2. `getajob vector add` — Adds a new search vector
   - Interactive prompts: name, keywords (comma-separated), location (optional), remote (y/n, default y)
   - Appends to search_vectors in config/settings.yaml
   - Use the existing yaml loading/saving pattern from cli.py's _write_default_search_vectors()
   - Print confirmation with the new vector details

Use the existing `app` Typer instance and create a `vector` command group using `typer.Typer(name="vector")` as a sub-app, then add it to the main app with `app.add_typer(vector_app)`.

IMPORTANT: Read cli.py fully first — it's 550+ lines. The vector commands should be added cleanly without breaking existing commands.

After adding, run: python3 -m pytest tests/ -q --tb=short
Then run: python3 -m getajob.cli vector list --help
