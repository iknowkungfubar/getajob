You are doing a practical end-to-end smoke test of the GetAJob project at /home/turin/getajob. A new user should be able to install and use this software. Test EVERYTHING and fix any issues you find.

=== TEST PLAN ===

1. INSTALL TEST (fresh venv)
   - Create a temp directory outside the project
   - Create a fresh venv, activate it
   - pip install the package from the source directory
   - Verify `python3 -m getajob.cli --help` works with no errors
   - Clean up the temp venv

2. SETUP TEST
   - Run `GETAJOB_DATABASE__URL=sqlite+aiosqlite:///test.db python3 -m getajob.cli setup` from the project dir
   - Verify it creates the data directory and DB tables
   - Check the exit code is 0
   - Clean up the test.db

3. PROFILE TEST
   - Check if there's a CLI command to create a profile
   - If not, verify the profile engine works by importing and instantiating ProfileStore
   - Verify ProfileStore.create_profile() works with in-memory SQLite

4. SERVE TEST (startup only)
   - Start `GETAJOB_DATABASE__URL=sqlite+aiosqlite:///test.db GETAJOB_SECURITY__ENCRYPTION_KEY=aaabbbcccdddeeefff00112233445566778899aabbccddeeff0011223344556677 python3 -m getajob.cli serve --port 19876` in background
   - Wait 5 seconds, curl http://localhost:19876/api/health
   - Verify response is 200 with status=ok
   - Verify curl http://localhost:19876/ redirects to /login (authentication check)
   - Kill the server
   - Clean up

5. INSTALL TEST (pip install)
   - Build the package: python3 -m build
   - Create a temp venv and pip install the built wheel
   - Verify the CLI works
   - Clean up

6. DOCKER CHECK
   - Run `python3 -c "import yaml; yaml.safe_load(open('docker-compose.yml'))"` to verify syntax
   - Check Dockerfile has no syntax errors with `python3 -c "open('Dockerfile').read()"`
   - Verify the docker-compose.yml references the correct env vars for the app

7. FIX ANY ISSUES FOUND
   - If any step fails, fix the underlying issue
   - Re-test after fixing
   - Document what was fixed

=== REPORT ===
After testing, report in /home/turin/getajob/INSTALL_TEST.md:
- What worked
- What was broken
- What was fixed
- Any remaining issues
